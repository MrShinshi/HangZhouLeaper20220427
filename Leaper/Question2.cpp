#ifdef DEBUG

/*! 感谢您参与利珀科技笔试，题目说明如下：
* [Q2]
* 使用 QGraphicView 完成小球在屏幕上匀速滚动碰壁反弹的一个小动画。
* 要求：1.必须使用 QGraphicView 和 QGraphicsScene 用于效果展示
*       2.小球可以用任意形式的 QGraphicsItem 代替
*       3.可以使用动画，也可以使用计时器与移动结合或其他的方式，只要能实现最终效果
*       4.可以求助于 Qt 帮助文档和网络，但不能直接使用网上的代码
*/

#include <QGraphicView>
#include <QGraphicsScene>













#endif // DEBUG